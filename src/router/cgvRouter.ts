import express, { Router } from "express";
// import boxOffice from "../api/boxOffice.ts";
// import detail from "../api/detail.ts";
// import movie from "../api/movie.ts";

const router: Router = express.Router();

// router.get("/", boxOffice);
// router.get("/", detail);
// router.get("/", movie);

export default router;
